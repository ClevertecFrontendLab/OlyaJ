export type SignUp = {
    email: string;
    login: string;
    password: string;
    firstName: string;
    lastName: string;
};

export type AuthResponse = {
    statusText: string;
    message: string;
};
